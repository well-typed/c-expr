exclude_path = [
  # copied from .gitignore because lychee does not respect .gitignore when
  # passed globs (see https://github.com/lycheeverse/lychee/discussions/1612).
  "dist-newstyle*",
]

accept = ["100..=103", "200..=299", 502]
